/*
 * Function: randomNumber
 * Usage: randomNumber(int min, int max)
 * -------------------------
 * This function takes two parameters whicha are the min and max values and returns a random number
 * between those values.
 */

#ifndef RANDOMNUMBER_HPP
#define RANDOMNUMBER_HPP

int randomNumber(int min, int max);         //Creates a random number between min&max parameters

#endif
